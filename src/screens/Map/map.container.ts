import { connect } from 'react-redux';
import MapComponent from './map.view';

export const MapContainer = connect(
  (state: any) => {
    return {
    }
  },
  (dispatch: any) => {
    return {
    }
  }
)(MapComponent);
