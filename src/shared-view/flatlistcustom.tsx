import React from 'react';
import {
  TouchableOpacity, 
  Text, 
  StyleSheet,
  FlatList,
} from 'react-native';

const FlatListCustom = ({onPress}: any) => {
  return (<></>);
}

const styles = StyleSheet.create({

})

export {FlatListCustom};
